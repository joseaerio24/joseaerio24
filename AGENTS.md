# AGENTS.md

## Cursor Cloud specific instructions

This repository (`joseaerio24/joseaerio24`) is a **GitHub profile README** repository. It contains only a `README.md` that GitHub renders on the user's profile page.

### Key facts

- There is no application code, no services, no dependencies, and no build/test/lint tooling.
- The only file is `README.md` (Markdown).
- No package manager, no runtime, and no external services are required.
- Changes are validated by reviewing the Markdown content directly; there is nothing to build or run.

### Development workflow

1. Edit `README.md` with the desired profile content.
2. Commit and push to `main`. GitHub will render it on the profile page automatically.
